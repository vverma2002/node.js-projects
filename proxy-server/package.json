{
  "name": "proxy-server",
  "version": "1.0.0",
  "description": "cors-anywhere proxy server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "proxy",
    "server"
  ],
  "author": "Vikrant Verma",
  "license": "ISC",
  "dependencies": {
    "cors-anywhere": "^0.4.4"
  }
}
